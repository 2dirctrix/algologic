TIL - 25/07/18

## 오늘 한 일

- 테이블 컨벤션 지정
- ERD 작성


##  문제가 있었던 것

- WebRTC를 활용했을 경우 백엔드와 프론트의 데이터 요청 부분에서 어느 정도까지 전달해야 하는지 파악이 안 되는 부분
- 테이블을 분리하지 않고 하나로 뒀을 경우 중복된 데이터가 많이 생기기 때문에 테이블 분리에 대한 협의
- 백엔드에서 어디까지 데이터를 저장하고, 데이터 가공에 대한 작업은 프론트엔드, 백엔드 중 어느 쪽에서 담당할지
- 컬럼명 정하기

## 새로 알게 된 것

- 모든 테이블에 created_at, updated_at을 추가하면 시간 정보가 기록되어 감사와 추적의 수단으로 사용 가능
- 테이블에 enum 컬럼을 적용하지 않고 문자열로 둔 다음 enum은 자바에서 관리
    - 이러면 굳이 DB를 거치지 않아도 되는 상황이 생기고, 추가로 테이블을 만들지 않아도 됨


## 앞으로 계획

- JPA 사용법 학습
- WebRTC를 프로젝트에 적용하기 위한 흐름과 방법 확인